# Extending DDPG to TD3 and Beyond

### Setup for running TD3
```bash
pip install -e .
cd TD3
python main_td3.py
```

### Environments
Currently set up to use gymnasium continuous environments, specifically LunarLanderContinuous-v3, which can be computationally expensive to run.

